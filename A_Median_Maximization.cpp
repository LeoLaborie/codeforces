#include <bits/stdc++.h>

using namespace std;

int main()
{
    int t;
    cin >> t;
    for (int i = 0; i < t; ++i)
    {
        uint n, s;
        cin >> n >> s;
        n;
        uint indice, median;
        indice = (n-1)/2 ;
        
        int nb_1 = n-indice;
    }
    
    return 0;
}